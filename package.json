{
  "name": "sunshinetour-webapp",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed - Vercel handles TypeScript compilation'",
    "deploy": "vercel --prod",
    "git:commit": "git add . && git commit -m",
    "git:status": "git status",
    "git:log": "git log --oneline"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.0",
    "@vercel/node": "^5.5.16",
    "@vercel/postgres": "^0.10.0",
    "formidable": "^3.5.4"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
